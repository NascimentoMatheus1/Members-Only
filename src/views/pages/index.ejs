<!DOCTYPE html>
<html lang="pt-br">

<%- include('../partials/head') %>

    <body>
        <%- include('../partials/navBar') %>
            <main>
                <div class="notice">
                    <% if (locals.currentUser && !currentUser.membership_status) { %>
                        <p><a href="/message/new">Crie uma nova postagem</a></p>
                        <p>
                            <a href="/user/membership">Torne-se um membro</a> para visualizar autores das postagens</a>
                        </p>
                        <% } else if (locals.currentUser && currentUser.membership_status) { %>
                            <p>Bem vindo de volta membro</p>
                            <p><a href="/message/new">Crie uma nova mensagem</a></p>
                            <% } else { %>
                                <p>
                                    <a href="/user/login">Login</a> ou <a href="/user/sign-up">Criar conta</a> para
                                    criar postagens e visualizar
                                    autores.
                                </p>
                                <% }%>

                </div>
                <div class="posts-container">
                    <% if (locals.currentUser && currentUser.membership_status) { %>
                        <% posts.map( (post)=> { %>
                            <div class="message-card">
                                <div class="message-header">
                                    <div class="user-info">
                                        <span class="full-name">
                                            <%= post.first_name + post.last_name %>
                                        </span>
                                        <span class="username">@<%= post.username %></span>
                                        <span class="dot">•</span>
                                        <span class="created-at">
                                            <%= post.create_at %>
                                        </span>
                                    </div>
                                </div>

                                <div class="message-body">
                                    <h3 class="title">
                                        <%= post.title %>
                                    </h3>
                                    <p class="content">
                                        <%= post.content %>
                                    </p>
                                </div>
                                <%if(currentUser.isAdmin){%>
                                    <form action="/message/delete/<%= post.id %>" method="POST">
                                        <button type="submit" class="delete-btn" title="Delete"
                                            onclick="return confirm('Are you sure you want to delete this message?')">
                                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none"
                                                stroke="currentColor" stroke-width="2">
                                                <polyline points="3 6 5 6 21 6"></polyline>
                                                <path
                                                    d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2">
                                                </path>
                                            </svg>
                                        </button>
                                    </form>
                                    <%}%>
                            </div>


                            <% }) %>
                                <% } else { %>
                                    <% posts.map( (post)=> { %>
                                        <div class="message-card">
                                            <div class="message-header">
                                                <div class="user-info">
                                                    <span class="full-name">*** ********</span>
                                                    <span class="username">@***_***</span>
                                                    <span class="dot">•</span>
                                                    <span class="created-at">** ** **, ****</span>
                                                </div>
                                            </div>

                                            <div class="message-body">
                                                <h3 class="title">
                                                    <%= post.title %>
                                                </h3>
                                                <p class="content">
                                                    <%= post.content %>
                                                </p>
                                            </div>
                                        </div>
                                        <% }) %>
                                            <% } %>
                </div>
            </main>
    </body>

</html>